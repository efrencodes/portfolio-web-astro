---
import { Image } from 'astro:assets';
interface Props {
	title: string;
    description: string;
    cover: string;
}

const { title, description, cover } = Astro.props;
---

<section id="section-headline-post" class="pb-6 flex flex-row gap-5 items-end">
    <Image
		src={cover}
		alt={title}
		height="197"
		width="197"
		class="rounded-md"
	/>
    <div>
        <h1 class="text-3xl font-medium text-gray-900 leading-normal mt-5">
                {title}
        </h1>
        <p class="italic text-sm mt-2">{description}</p>
    </div>
</section>